# Static environment variables for BudAI Orchestrator
# These should be set once in Railway and don't need to change between deployments

# Service identity
BUDAI_SERVICE_NAME=orchestrator
BUDAI_SERVICE_VERSION=1.0.0

# Internal service URLs (Railway private networking)
BUDAI_AGENT_SUMMARIZER_URL=http://budai-agent-summarizer.railway.internal:8002
BUDAI_AGENT_FOLLOWUP_URL=http://budai-agent-followup.railway.internal:8003
BUDAI_AGENT_COMMUNICATOR_URL=http://budai-agent-communicator.railway.internal:8004

# Service configuration
BUDAI_POLL_INTERVAL_SECONDS=30
BUDAI_MAX_CONCURRENT_WORKFLOWS=10

# ==============================================================================
# DYNAMIC VARIABLES (Set via deployment script or Railway dashboard)
# ==============================================================================
# These variables change between environments and are set at deploy time:
#
# BUDAI_ENVIRONMENT=development|staging|production
# BUDAI_REDIS_URL=redis://...
# BUDAI_OPENAI_API_KEY=sk-...

